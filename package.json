{
  "name": "taas-monorepo",
  "private": true,
  "scripts": {
    "frontend:install": "npm install --prefix apps/frontend",
    "frontend:start": "npm start --prefix apps/frontend",
    "frontend:build": "npm run build --prefix apps/frontend",
    "frontend:test": "npm test --prefix apps/frontend",
    "gateway:install": "npm install --prefix services/frontend-gateway",
    "gateway:start": "npm start --prefix services/frontend-gateway"
  }
}
